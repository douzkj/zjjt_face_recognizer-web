  
// import { data } from 'react-router-dom';

// export const parseVisitorData = (dataString: string): Visitor[] => {
//     try {
//       const rawData = JSON.parse(dataString);
//       return rawData.map((item: any) => ({
//         id: String(item.id),
//         user_id: item.user_id,
//         face_img_path: item.face_img_path,
//         show_status: item.show_status as 0 | 1
//       }));
//     } catch (error) {
//       throw new Error('访客数据解析失败');
//     }
//   };

